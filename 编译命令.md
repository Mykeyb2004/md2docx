# macOS 独立应用编译命令

在 Apple Silicon（arm64）Mac 上，于终端执行下面的完整命令：

```bash
cd /Users/zhangqijin/PycharmProjects/md2docx && uv run --group build python scripts/build_nuitka.py --entry gui --mode app --clean
```

该命令会运行测试、使用 Nuitka 编译 GUI standalone `.app bundle`、打包 Tcl/Tk 和项目模板、执行本地 ad-hoc 签名与验证，并在验证成功后发布应用。

编译成功后的独立应用位于：

```text
/Users/zhangqijin/PycharmProjects/md2docx/dist/macos/Md2docx.app
```

启动应用：

```bash
open /Users/zhangqijin/PycharmProjects/md2docx/dist/macos/Md2docx.app
```

注意：当前管线仅支持 macOS Apple Silicon 本机构建，使用仓库固定的 Python 3.11.9。应用采用 standalone `.app bundle`，不是 onefile，因此启动时不需要先解压运行时；当前使用 ad-hoc 签名，适合本机运行，不包含 Developer ID 签名、公证、DMG 或 Universal 2 构建。

构建日志位于：

```text
/Users/zhangqijin/PycharmProjects/md2docx/build/logs/macos-app-build.log
```

详细背景与启动优化原则参见 [docs/MACOS_APP_STARTUP_OPTIMIZATION.md](docs/MACOS_APP_STARTUP_OPTIMIZATION.md)。
