# md2docx 配置文件字段说明

本文档基于 `md2docx/templates/default.yaml` 和当前代码实现整理，用来说明配置文件的结构、字段含义、可选值和生效范围。

## 配置文件如何被加载

md2docx 使用 YAML 作为样式配置文件。顶层必须是一个对象，也就是形如：

```yaml
document:
  page_size: A4
paragraph:
  font_name: "仿宋"
```

常见使用方式：

- 不传配置时：优先读取当前运行目录下的 `default.yaml`，如果不存在，则读取包内置的 `md2docx/templates/default.yaml`。
- 使用模板名时：例如 `--template default` 或 `StyleManager("default")`，会优先读取当前运行目录的 `default.yaml`，否则读取包内置模板。
- 使用自定义文件时：例如 `--style-file custom.yaml`，会直接读取该 YAML 文件。
- `--template` 和 `--style-file` 不能同时使用。

注意：直接指定自定义 YAML 文件时，代码不会先把它和 `default.yaml` 深度合并后再使用。建议复制 `md2docx/templates/default.yaml` 后再修改，避免遗漏字段导致部分样式使用代码内置兜底值。GUI 编辑器和运行时覆盖配置会使用递归合并，未知字段会被保留，但只有代码读取到的字段才会影响输出。

## 通用写法

### 值类型

- 字符串：`"仿宋"`、`A4`、`center` 均可；含特殊字符或中文字体名时建议加引号。
- 布尔值：`true` / `false`。
- 空值：`null`，常用于 metadata，表示使用程序默认推导。
- 数字：`1.5`、`80`。
- 列表：`keywords: ["报告", "投标"]`。
- 对象：缩进写子字段，例如 `inline.code.font_color`。

### 长度单位

多数长度字段支持：

- `pt`：磅，例如 `14pt`。
- `cm`：厘米，例如 `2.54cm`。
- `mm`：毫米，例如 `10mm`。
- `in`：英寸，例如 `0.5in`。

页边距字段不带单位时按厘米处理；渲染器中的段前、段后、图片间距等不带单位时通常按 pt 处理。建议始终写单位。

### 字号

字号字段支持整数或带 `pt` 的字符串，例如：

```yaml
font_size: 14pt
```

当前实现按整数磅解析，建议不要写 `10.5pt` 这类小数字号。

### 颜色

颜色支持以下写法：

```yaml
font_color: "#000000"
font_color: "000000"
font_color: "#D14"
```

`#RGB` 短格式会被展开为 `#RRGGBB`。

### 对齐

段落水平对齐常用值：

- `left`
- `center`
- `right`
- `justify`

表格单元格垂直对齐常用值：

- `top`
- `center`
- `bottom`

未知值通常会回退为左对齐或顶部对齐。

## 顶层结构总览

`default.yaml` 当前包含这些顶层配置块：

```yaml
document:      # 页面、分割线、表格修复等文档级设置
metadata:      # Word 文档属性
heading1:      # Markdown 一级标题
heading2:      # Markdown 二级标题
heading3:      # Markdown 三级标题
heading4:      # Markdown 四级及更深标题
outline:       # 中文公文式大纲标题覆盖样式
paragraph:     # 普通段落
inline:        # Markdown 行内加粗、斜体、行内代码
code_block:    # fenced code block
table:         # Markdown 表格
list:          # Markdown 列表
math_inline:   # 行内公式图片配置，当前大多为预留字段
math_block:    # 块级公式配置
mermaid:       # Mermaid 图渲染和排版
chapter_scan:  # 章节 Markdown 后置扫描脚本配置
```

## document

文档级设置。主要由 `Converter._apply_document_settings()`、`MarkdownParser` 和 `DocxRenderer.thematic_break()` 使用。

```yaml
document:
  page_size: A4
  margin_top: 2.54cm
  margin_bottom: 2.54cm
  margin_left: 3.17cm
  margin_right: 3.17cm
  line_spacing: 1.5
  ignore_thematic_breaks: true
  auto_fix_tables: false
  section_index: false
```

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `page_size` | `A4` / `A3` / `Letter` | 设置 Word 第一节页面尺寸。大小写不敏感。 |
| `margin_top` | `2.54cm` | 上页边距。支持 `cm`、`mm`、`in`、`pt`。 |
| `margin_bottom` | `2.54cm` | 下页边距。 |
| `margin_left` | `3.17cm` | 左页边距。 |
| `margin_right` | `3.17cm` | 右页边距。 |
| `line_spacing` | `1.5` | 索引页标题未配置行距时的回退值。正文、表格、列表请分别配置 `paragraph.line_spacing`、`table.line_spacing`、`list.line_spacing`。 |
| `ignore_thematic_breaks` | `true` / `false` | 是否忽略 Markdown 分割线 `---`、`***` 等。`true` 表示不写入 docx；`false` 会渲染为一条水平线。 |
| `auto_fix_tables` | `true` / `false` | 是否尝试修复缺少 separator 行的高置信度 Markdown 表格。GUI 主界面的相关开关会运行时覆盖这个值。 |
| `outline_mode` | `auto` / `on` / `off` | 可选字段，默认不写在 `default.yaml` 中。控制中文公文式大纲识别：`auto` 自动判断，`on` 强制开启，`off` 关闭。 |
| `section_index` | `true` / `false` | 默认关闭。在每个二级标题前插入独立索引页：双线框表格第一行为二级标题，下方每行列出一个所属三级或四级标题，分别复用 `heading2`、`heading3`、`heading4` 的字体、字号和段落排版。标题与首个条目保持同页，表格按内容自适应，内部不显示横线。条目可跳转且不显示页码，模板页眉页脚和页码规则保留。无下级标题时仅保留标题行；长索引在条目间续页，正文另起一页。主界面开关可覆盖本次转换并自动记忆；打开或保存配置后按配置同步。 |

## metadata

Word 文档属性。由 `Converter._apply_core_properties()` 在生成文档后写入。

```yaml
metadata:
  author: null
  last_modified_by: null
  title: null
  subject: null
  keywords: []
  comments: null
  category: null
```

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `author` | `"张三"` / `null` | 作者。为 `null` 或空值时，使用当前系统用户；取不到时使用 `md2docx`。 |
| `last_modified_by` | `"张三"` / `null` | 最后修改者。为空时使用 `author`。 |
| `title` | `"项目报告"` / `null` | 标题。为空时优先从 Markdown 第一个标题提取，再从文档首段文字推导。 |
| `subject` | 字符串 / `null` | Word 属性中的主题。为空时写入空字符串。 |
| `keywords` | 列表或字符串 | 关键词。列表会用英文逗号和空格拼接，例如 `["A", "B"]` 写为 `A, B`。 |
| `comments` | 字符串 / `null` | Word 属性中的备注。 |
| `category` | 字符串 / `null` | Word 属性中的类别。 |

`created` 和 `modified` 不在配置中暴露，程序会自动写入当前 UTC 时间。

## heading1 到 heading4

Markdown 标题样式。`#` 使用 `heading1`，`##` 使用 `heading2`，`###` 使用 `heading3`，`####` 以及更深标题使用 `heading4`。

```yaml
heading1:
  font_name: "方正小标宋简体"
  font_size: 22pt
  font_color: "#000080"
  bold: true
  space_before: 6pt
  space_after: 6pt
  alignment: center
```

`heading2`、`heading3`、`heading4` 支持同样字段。

索引页也直接读取对应级别的标题配置，不另设一套字体字号。字体、字号、颜色、粗斜体、对齐、首行缩进、段前段后距和 `line_spacing` 均可复用；字号支持 `10.5pt` 等小数。在 GUI 保存配置后，下次启动恢复上次配置文件。

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `font_name` | `"黑体"` | 标题字体，同时设置 East Asian 字体。 |
| `font_size` | `16pt` | 标题字号。 |
| `font_color` | `"#000000"` | 标题颜色。 |
| `bold` | `true` / `false` | 是否加粗。标题级配置会覆盖标题内所有 run 的加粗状态。 |
| `italic` | `true` / `false` | 是否斜体。`heading4` 默认示例中包含该字段，其他标题也可写。 |
| `space_before` | `6pt` | 段前间距。 |
| `space_after` | `6pt` | 段后间距。 |
| `alignment` | `left` / `center` / `right` / `justify` | 标题段落对齐。 |
| `first_line_indent` | `2` | 首行缩进字符数。实现方式为 `字符数 * 当前字号 pt`。小于等于 0 时不设置。 |

## outline

中文公文式大纲标题的样式覆盖。由 `document.outline_mode` 控制是否启用。

代码识别这些行首格式：

- 一级：`一、`
- 二级：`（一）` 或 `(一)`
- 三级：`1.`
- 四级：`（1）` 或 `(1)`

默认样式映射：

| 大纲级别 | 默认引用样式 |
| --- | --- |
| `level1` | `heading2` |
| `level2` | `heading3` |
| `level3` | `heading4` |
| `level4` | `paragraph` |

`default.yaml` 中的配置：

```yaml
outline:
  level2:
    bold: false
```

支持写法：

```yaml
outline:
  level1_style: heading2
  level2_style: heading3
  level3_style: heading4
  level4_style: paragraph
  level2:
    bold: false
    font_name: "楷体"
```

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `level1_style` 到 `level4_style` | `heading2` / `paragraph` / 自定义顶层样式名 | 指定某一级大纲先继承哪个样式块。 |
| `level1` 到 `level4` | 对象 | 在继承样式基础上覆盖字段，例如只覆盖 `bold`、`font_name`、`alignment`。 |

## paragraph

普通段落样式。由 `DocxRenderer.paragraph()` 使用。

```yaml
paragraph:
  font_name: "仿宋"
  font_size: 14pt
  line_spacing: 1.5
  first_line_indent: 2
  alignment: justify
  space_before: 0pt
  space_after: 0pt
```

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `font_name` | `"仿宋"` | 正文字体。 |
| `font_size` | `14pt` | 正文字号。 |
| `font_color` | `"#000000"` | 可选字段，默认模板未写；写入后会作为普通文本颜色。 |
| `line_spacing` | `1.5` | 段落行距。 |
| `first_line_indent` | `2` | 首行缩进字符数，按当前字号换算为 pt。 |
| `alignment` | `left` / `center` / `right` / `justify` | 段落对齐方式。 |
| `space_before` | `0pt` | 段前间距。 |
| `space_after` | `0pt` | 段后间距。 |

## inline

Markdown 行内样式。由 `_add_formatted_text()` 使用。

```yaml
inline:
  bold:
    font_color: null
  italic:
    font_color: null
  code:
    font_name: "Consolas"
    font_size: 11pt
    font_color: "#D14"
    background: "#F5F5F5"
```

### inline.bold

用于 `**加粗**` 或 `__加粗__`。

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `font_color` | `null` / `"#000000"` | 加粗文本颜色。`null` 表示继承当前位置原颜色。 |

### inline.italic

用于 `*斜体*` 或 `_斜体_`。

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `font_color` | `null` / `"#666666"` | 斜体文本颜色。`null` 表示继承当前位置原颜色。 |

### inline.code

用于 Markdown 行内代码 `` `code` ``。

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `font_name` | `"Consolas"` | 行内代码字体。 |
| `font_size` | `11pt` | 行内代码字号。 |
| `font_color` | `"#D14"` | 行内代码文字颜色。 |
| `background` | `"#F5F5F5"` | 行内代码背景色。 |

## code_block

Fenced code block 样式。由 `DocxRenderer.block_code()` 使用。`latex` / `tex` 代码块会优先按公式处理，`mermaid` 代码块会优先按 Mermaid 图处理；渲染失败时才回退为普通代码块。

```yaml
code_block:
  font_name: " 宋体"
  font_size: 14pt
  bold: true
  font_color: "#24292E"
  background: "#F6F8FA"
  border_color: "#D0D7DE"
  line_spacing: 1.5
  space_before: 6pt
  space_after: 6pt
  padding: 12pt
```

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `font_name` | `"宋体"` / `"Consolas"` | 代码块字体。 |
| `font_size` | `14pt` | 代码块字号。 |
| `bold` | `true` / `false` | 是否加粗。 |
| `italic` | `true` / `false` | 可选字段，默认模板未写；写入后可设置斜体。 |
| `font_color` | `"#24292E"` | 代码文字颜色。 |
| `background` | `"#F6F8FA"` | 代码块段落背景色。 |
| `border_color` | `"#D0D7DE"` | 当前模板字段，渲染代码尚未读取，暂不会生成边框。 |
| `line_spacing` | `1.5` | 代码块行距。 |
| `space_before` | `6pt` | 代码块段前间距。 |
| `space_after` | `6pt` | 代码块段后间距。 |
| `padding` | `12pt` | 近似内边距。当前实现通过在每行前添加空格模拟，不是 Word 原生 padding。 |

## table

Markdown 表格样式。由 `DocxRenderer.table()`、`table_head()`、`table_row()` 使用。

```yaml
table:
  style: "Light Grid Accent 1"
  font_name: "仿宋"
  font_size: 12pt
  line_spacing: 1.5
  header_bold: true
  header_background: "#F2F2F2"
  header_alignment: center
  vertical_alignment: center
  cell_margin_vertical: 3pt
  cell_margin_horizontal: 5.4pt
  header_vertical_alignment: center
  column_width_strategy: content-weighted
  border_color: "#CCCCCC"
  alignment: left
  alternating_rows: false
  row_background_odd: "#FFFFFF"
  row_background_even: "#F9F9F9"
```

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `style` | `"Light Grid Accent 1"` | Word 表格样式名。若本机 Word 模板不存在该样式，代码会忽略错误并使用默认样式。 |
| `font_name` | `"仿宋"` | 表格单元格文字字体。 |
| `font_size` | `12pt` | 表格单元格文字字号。 |
| `line_spacing` | `1.5` | 表格单元格段落行距。 |
| `header_bold` | `true` / `false` | 当前模板字段，但表头目前总是加粗，代码未读取该布尔值。 |
| `header_background` | `"#F2F2F2"` | 表头单元格背景色。 |
| `header_alignment` | `left` / `center` / `right` / `justify` / `inherit` | 表头水平对齐。默认 `center`。设为 `inherit` 时跟随 Markdown 列对齐或 `table.alignment`。 |
| `vertical_alignment` | `top` / `center` / `bottom` | 表体单元格垂直对齐。 |
| `cell_margin_vertical` | `3pt` | 单元格上下内边距。 |
| `cell_margin_horizontal` | `5.4pt` | 单元格左右内边距。 |
| `header_vertical_alignment` | `top` / `center` / `bottom` | 表头单元格垂直对齐。 |
| `column_width_strategy` | `content-weighted` / `balanced` | 列宽策略。`content-weighted` 按内容长度分配，`balanced` 更均衡保守。 |
| `border_color` | `"#CCCCCC"` | 当前模板字段，渲染代码尚未读取，暂不会修改边框颜色。 |
| `alignment` | `left` / `center` / `right` / `justify` | 表体单元格默认水平对齐；Markdown 表格列对齐优先级更高。 |
| `alternating_rows` | `true` / `false` | 是否启用表体斑马纹。 |
| `row_background_odd` | `"#FFFFFF"` | 奇数表体行背景色。表头不计入表体行。 |
| `row_background_even` | `"#F9F9F9"` | 偶数表体行背景色。 |

## list

Markdown 列表样式。由 `DocxRenderer._render_list_item()` 使用。

```yaml
list:
  font_name: "仿宋"
  font_size: 14pt
  line_spacing: 1.5
  bullet_char: "•"
  number_format: "1."
  ordered_list_as_text: false
  indent_size: 0.5in
  space_after: 3pt
```

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `font_name` | `"仿宋"` | 列表项文字字体。 |
| `font_size` | `14pt` | 列表项文字字号。 |
| `line_spacing` | `1.5` | 列表项行距。 |
| `bullet_char` | `"•"` / `"-"` / `"◆"` | 无序列表符号。默认 `•` 使用 Word 的 `List Bullet` 样式；其他字符会作为普通文本加在列表项前。 |
| `number_format` | `"1."` / `"1)"` / `"(1)"` | 有序列表编号格式。仅在 `ordered_list_as_text: true` 时用于生成文本编号；自动编号模式下目前使用 Word `List Number`。 |
| `ordered_list_as_text` | `true` / `false` | `true` 表示把有序列表写成普通文本，例如 `1. 内容`；`false` 表示使用 Word 自动编号。 |
| `indent_size` | `0.5in` | 嵌套列表每级缩进。当前实现只在 `depth > 0` 的嵌套列表上显式设置，且只解析以 `in` 结尾的字符串。 |
| `space_after` | `3pt` | 列表项段后间距。 |

## math_inline

行内公式配置。Markdown 中的 `$...$` 会优先转换为 Word OMML；失败时回退为图片。

```yaml
math_inline:
  height: 0.15in
  dpi: 300
```

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `height` | `0.15in` | 当前模板字段；回退图片高度在代码中固定为 `0.15in`，尚未从配置读取。 |
| `dpi` | `300` | 当前模板字段；数学图片转换器目前在渲染器中固定以 `300` 初始化，尚未从配置读取。 |

## math_block

块级公式配置。`$$...$$` 和 `latex` / `tex` fenced code block 会优先转换为 Word OMML；失败时回退为图片。

```yaml
math_block:
  width: 4in
  alignment: center
  space_before: 6pt
  space_after: 6pt
  dpi: 300
```

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `width` | `4in` | 块级公式回退为图片时的图片宽度。OMML 成功时不使用该字段。 |
| `alignment` | `left` / `center` / `right` | 块级公式段落对齐。未知值回退为居中。 |
| `space_before` | `6pt` | 块级公式段前间距。 |
| `space_after` | `6pt` | 块级公式段后间距。 |
| `dpi` | `300` | 当前模板字段；数学图片转换器目前固定以 `300` 初始化，尚未从配置读取。 |

## mermaid

Mermaid 图配置。Markdown 中 fenced code block 的语言为 `mermaid` 时生效：

````markdown
```mermaid
flowchart TD
  A --> B
```
````

```yaml
mermaid:
  command: mmdc
  format: png
  theme: base
  width: 5.5in
  alignment: center
  space_before: 6pt
  space_after: 6pt
  background_color: white
  theme_variables:
    primaryColor: "#E8F1FF"
    primaryTextColor: "#163A70"
    primaryBorderColor: "#2F6BFF"
    lineColor: "#2F6BFF"
    secondaryColor: "#F4F9FF"
    tertiaryColor: "#EEF3F8"
    clusterBkg: "#F7FBFF"
    clusterBorder: "#9AB8E8"
    edgeLabelBackground: "#FFFFFF"
  soft_max_height_ratio: 0.68
  hard_max_height_ratio: 0.82
  page_max_height_ratio: 0.92
  page_break_threshold_ratio: 0.90
  min_readable_width: 3.2in
  oversized_strategy: page
  force_page_break_before_oversized: false
  keep_with_previous: true
  keep_with_previous_max_chars: 80
  follow_previous_trigger_height_ratio: 0.24
  follow_previous_width_ratio: 0.55
  follow_previous_space_before: 0pt
  keep_together: true
  keep_with_next: false
  widow_control: false
```

### 渲染命令字段

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `command` | `mmdc` | Mermaid CLI 命令或可执行文件路径。需要本机能运行 Mermaid CLI。 |
| `format` | `png` / `svg` / `pdf` | 传给 Mermaid 转换器的输出格式。默认模板用 `png`；Word 插图路径最稳妥的是 PNG。 |
| `theme` | `default` / `base` / `dark` / `forest` / `neutral` | Mermaid 主题。 |
| `theme_variables` | 对象 | Mermaid `themeVariables`。只要配置了该对象，转换器会生成 Mermaid 配置文件，并把 Mermaid 主题强制设为 `base` 以保证变量生效。 |
| `background_color` | `white` / `"#FFFFFF"` | Mermaid 输出图背景色。 |
| `scale` | 数字，例如 `2` | 可选字段，默认模板未写；传给 Mermaid CLI 的缩放参数。 |

### Word 排版字段

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `width` | `5.5in` | Mermaid 图首选宽度。最终不会超过当前页面可用宽度。 |
| `alignment` | `left` / `center` / `right` | 图所在段落的水平对齐。 |
| `space_before` | `6pt` | 图前间距。 |
| `space_after` | `6pt` | 图后间距。 |
| `hard_max_height_ratio` | `0.82` | 图高度超过页面可用高度的该比例时，会按比例缩小。 |
| `page_max_height_ratio` | `0.92` | `oversized_strategy: page` 时，超大图在页面布局模式下允许使用的最大高度比例。 |
| `page_break_threshold_ratio` | `0.90` | `oversized_strategy: page` 时，首选高度超过该阈值会进入页面布局模式。 |
| `min_readable_width` | `3.2in` | `oversized_strategy: page` 时，如果按硬高度缩小后宽度低于该值，也会进入页面布局模式。 |
| `oversized_strategy` | `page` / `scale` | `page` 会启用页面布局模式；非 `page` 值会按硬高度约束缩放，不额外启用页面布局模式。 |
| `force_page_break_before_oversized` | `true` / `false` | 在页面布局模式下是否强制图前分页。默认 `false`，表示尽量不硬分页。 |
| `soft_max_height_ratio` | `0.68` | 当前会被计算为软限制标记，但返回值未被后续渲染逻辑使用，暂无直接可见效果。 |

### 与前段绑定和分页控制

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `keep_with_previous` | `true` / `false` | 是否允许小图和前一个短段落保持在一起。 |
| `keep_with_previous_max_chars` | `80` | 前段文本长度不超过该字符数时，才允许绑定；小于等于 0 表示不限制长度。 |
| `follow_previous_trigger_height_ratio` | `0.24` | 图高度不超过页面可用高度的该比例时，才会进入“跟随前段”的紧凑布局。 |
| `follow_previous_width_ratio` | `0.55` | 跟随前段时，图宽最多按页面可用宽度的该比例收窄。 |
| `follow_previous_space_before` | `0pt` | 跟随前段时使用的图前间距。 |
| `keep_together` | `true` / `false` | 设置图段落的 Word keep-together。 |
| `keep_with_next` | `true` / `false` | 设置图段落与后一段保持在一起。 |
| `widow_control` | `true` / `false` | 设置图段落的 Word widow control。 |

## chapter_scan

章节 Markdown 后置扫描脚本配置，用于 `scripts/scan_chapter_issues.py` / `chapter_issue_scanner`，不影响 docx 渲染。

```yaml
chapter_scan:
  target_dir: output
  glob: "*.md"
  recursive: true
```

| 字段 | 类型 / 示例 | 说明 |
| --- | --- | --- |
| `target_dir` | `output` | 扫描目标目录。相对路径会相对于配置文件所在目录解析；没有配置文件时相对于当前工作目录。 |
| `glob` | `"*.md"` | 文件匹配模式。 |
| `recursive` | `true` / `false` | 是否递归扫描子目录。 |

扫描器还会优先尝试读取顶层业务字段 `root_dir` + `output_dir` 来推导扫描目录；命令行传入的 `target_dir` / `pattern` 优先级更高。

## 最小可用配置示例

如果只想调整正文、标题和页面，可以这样写：

```yaml
document:
  page_size: A4
  margin_top: 2.54cm
  margin_bottom: 2.54cm
  margin_left: 3.17cm
  margin_right: 3.17cm
  ignore_thematic_breaks: true
  auto_fix_tables: false

heading1:
  font_name: "方正小标宋简体"
  font_size: 22pt
  bold: true
  alignment: center

paragraph:
  font_name: "仿宋"
  font_size: 14pt
  line_spacing: 1.5
  first_line_indent: 2
  alignment: justify
  space_before: 0pt
  space_after: 0pt
```

更稳妥的做法是复制完整 `md2docx/templates/default.yaml` 后修改，因为部分字段在某个配置块存在但子字段缺失时，不一定会自动回填 `default.yaml` 中的同级默认值。

## 当前模板字段生效状态速查

| 配置块 | 完全或主要生效字段 | 当前预留 / 部分生效字段 |
| --- | --- | --- |
| `document` | `page_size`、`margin_*`、`ignore_thematic_breaks`、`auto_fix_tables`、可选 `outline_mode` | `line_spacing` 暂无文档级直接效果 |
| `metadata` | 全部字段 | 无 |
| `heading1-4` | `font_name`、`font_size`、`font_color`、`bold`、`italic`、`space_before`、`space_after`、`alignment`、`first_line_indent` | 无 |
| `outline` | `levelN_style`、`levelN` 覆盖 | 无 |
| `paragraph` | `font_name`、`font_size`、`font_color`、`line_spacing`、`first_line_indent`、`alignment`、`space_before`、`space_after` | 无 |
| `inline` | `bold.font_color`、`italic.font_color`、`code.*` | 无 |
| `code_block` | `font_name`、`font_size`、`bold`、`italic`、`font_color`、`background`、`line_spacing`、`space_before`、`space_after`、`padding` | `border_color` 暂未读取 |
| `table` | `style`、`font_name`、`font_size`、`line_spacing`、`header_background`、`header_alignment`、`vertical_alignment`、`cell_margin_*`、`header_vertical_alignment`、`column_width_strategy`、`alignment`、`alternating_rows`、`row_background_*` | `header_bold` 暂未读取且表头总是加粗；`border_color` 暂未读取 |
| `list` | `font_name`、`font_size`、`line_spacing`、`bullet_char`、`ordered_list_as_text`、`indent_size`、`space_after` | `number_format` 仅在 `ordered_list_as_text: true` 时影响文本编号 |
| `math_inline` | 当前行为等同默认值 | `height`、`dpi` 暂未从配置读取 |
| `math_block` | `alignment`、`space_before`、`space_after`；`width` 在图片回退时生效 | `dpi` 暂未从配置读取 |
| `mermaid` | 大多数字段生效 | `soft_max_height_ratio` 当前仅计算，暂无直接可见效果 |
| `chapter_scan` | 全部字段 | 仅影响扫描脚本，不影响 docx 渲染 |
