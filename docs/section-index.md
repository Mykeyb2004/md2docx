# 二级标题索引页

在 GUI 主界面勾选「插入二级标题索引页」即可生效，开关自动记住下次使用。也可在「编辑配置 → 文档」或 YAML 中设置：

```yaml
document:
  section_index: true
```

默认值为 `false`，旧配置没有该字段时保持原来的转换结果。命令行通过 `--style-file` 加载配置；Python API 可通过 `config_override` 或 `config_data` 设置相同字段。

## 样式和保存

直接使用现有「一级～四级标题」转换配置：索引页标题使用 `heading2`，三级条目使用 `heading3`，四级条目使用 `heading4`。一级标题配置仍供正文使用，不额外加入索引。

复用字体、字号、颜色、粗体、斜体、对齐、首行缩进、段前段后距及行距；字号支持 `10.5pt`。标题未指定行距时索引使用 `document.line_spacing`（缺省为 1.5）。独立分页、双线框、框内垂直居中及三级标题与首个四级条目保持相邻由索引模块处理。

在编辑器中「保存」或「另存为」后，所有一级～四级标题设置都写入 YAML；应用记住配置路径，下次启动重新载入。主界面开关立即生效，单独保存在偏好中，不自动改写用户的 YAML。打开编辑器时会带入当前开关，保存时一并写入 YAML；打开其他配置或保存编辑器后，主界面与该配置同步。

保留已确认样例的页面结构，文字排版以用户配置为准。需要参考图中的居中效果时，将二～四级标题的对齐设为「居中」、首行缩进设为 0；分组留白可通过三级标题的段前距调整。

## 生成规则

- 每个二级标题前另起索引页，二级标题放在双线框表格的第一行，所属三级、四级标题各占后续一行；文字样式来自对应标题配置。表格启用内容自适应布局，高度由条目、字号和段落间距决定，不设固定高度或页面比例最小高度；列网格参考模板正文宽度，保留双线外框，内部不显示横线。左右及整个表格的上下外缘保留内边距，条目行之间仅使用配置的段落间距。
- 遇到下一个一级或二级标题时结束当前节。五级、六级标题不纳入索引，即使正文将它们显示为 Word 的四级标题样式。
- 索引条目不显示页码，也不需要更新目录域；模板页脚中的页码正常保留。索引条目可跳转到正文对应标题；同名标题仍有独立目标。
- 没有三级、四级标题时，索引表格只有第一行的二级标题，不生成空的条目行。
- 索引页的二级标题与表格起始部分保持同页，不单独占一页。索引独立于正文，正文中的二级标题另起一页。超过一页的长索引自动续页，不截断条目或缩小用户指定字号。
- 索引文字保留原标题内容，作为普通段落写入，不重复进入 Word 导航窗格或总目录。参考图中的蓝色换行标记是编辑器辅助标记，不会写入。
- 文档有 Markdown 章节标题（`##`～`######`）时，只使用 Markdown 标题结构生成索引；正文中的 `一、`、`（一）` 等中文编号保留原排版，不生成额外索引页，也不截断章节索引。
- 没有 Markdown 章节结构时，兼容现有中文大纲识别结果；允许文档开头只有 `#` 总标题。此时中文大纲层级沿用识别器定义：`一、` 为一级、`（一）` 为二级、`1.` 为三级、`（1）` 为四级，与配置中选择的外观样式无关。
- 直接在载入的 Word 模板文档中插入，保留页面尺寸、页边距、页眉页脚距离、首页/奇偶页设置、页码域及起始页码，不额外创建节。模板仍遵循转换器现有的单节限制。

## 实现与影响分析

渲染器按顺序记录标题的语义层级及段落引用；`Converter.to_document()` 在正文和元数据完成后按开关调用独立的 `section_index` 模块。该模块单次分组，插入普通段落、内部书签及跳转链接，不重新解析 Markdown。每次转换创建新的渲染器，记录不会跨文档累积。

渲染器另行记录 Markdown 标题来源，转换入口据此选择单一章节结构。这样可避免中文正文小标题被误当成独立章节、生成仅有标题的索引页并强制分页。回归测试覆盖自动/强制中文大纲识别、有/无 Word 模板、正文段落格式不变、索引跨中文编号持续收集，以及纯中文大纲和转换器重复使用。

GitNexus 编辑前评估：

| 修改点 | 风险 | 直接影响 |
| --- | --- | --- |
| `Converter.to_document` | LOW | `convert_string`，间接关联 `convert` |
| `DocxRenderer.__init__`、`heading` | LOW | 静态图谱未识别生产调用方；Mistune 的动态分派由转换测试覆盖 |
| `_render_outline_paragraph` | LOW | `paragraph`，关联段落字体、分派、公式和长度处理流程 |
| GUI 配置标签及提示 | LOW | 增加映射，由现有布尔值控件自动生成复选框 |
| GUI 主界面、打开/保存配置及转换配置合成 | LOW | `setup_ui`、`_do_conversion`、配置打开与偏好保存流程；复用原有 YAML 保存机制 |
| `_apply_text_paragraph_format` | LOW | 正文与中文大纲；支持显式零缩进。Markdown 标题也复用此方法以应用行距 |
| `_parse_font_size` | CRITICAL | 7 个直接引用、16 个上游符号，涉及标题、正文、列表、表格、代码和公式。只将整数解析改为浮点解析，避免用户选择 `10.5pt` 后转换失败；全量回归验证 |
| `StyleManager` 默认值 | CRITICAL（类级范围） | 32 个直接引用，其中 28 个为历史版本快照；当前引用为转换器、解析器、GUI 和章节扫描器。本次仅增加默认值，不改加载或合并逻辑 |

本地 GitNexus 图谱已刷新到实现前的 HEAD。全局仓库注册表更新因沙箱限制及自动审批服务 503 未完成；图谱查询和影响分析正常。

`detect_changes(scope="all")` 已执行，汇总风险为 critical；工具将同路径后缀的历史快照也计入变更，并存在行号偏移，需结合实际 `git diff` 判断。实际修改涉及转换入口、标题记录/格式、默认开关、GUI 开关与偏好及相关文档，新增索引模块及测试；历史快照没有修改。新模块尚未进入图谱，其 `impact` 返回 UNKNOWN，以已评估的转换入口和回归测试补充核验。

## 回归验证

`tests/test_section_index.py` 覆盖默认关闭、旧配置兼容、章节边界、空节、同名书签、双线框、保存后字体和段落参数的复用、代码块排除、中文大纲、重复转换、Word 模板和长索引。模板回归比较完整节属性、首页/奇偶页页眉页脚 XML 和页码域。`tests/test_gui.py` 验证主界面控件、开关覆盖、偏好写入失败提示、切换配置同步、字体配置保存并模拟重新启动后的实际转换。配置编辑器既有测试检查默认字段的中文标签与帮助文本。

```bash
uv run pytest tests/test_section_index.py tests/test_converter.py tests/test_renderer.py tests/test_outline_headings.py tests/test_gui.py tests/test_config_document.py tests/test_styles.py -q
```

2026-09-17 全量 `uv run pytest -q`：303 项通过；有一项既有的 `test_nested_lists` 返回值警告。DOCX 的配置、模板、分页、框线和书签结构已通过测试。用户已确认此前 `tests/section_index_tmp/sample.docx` 的第一页内容和版式。混合标题分页问题修复后，使用实际文档开头及原模板生成 `tests/section_index_tmp/pagination_fixed.docx`，确认只生成一个章节索引，中文正文标题不再被强制分页。

使用随 Codex 运行环境提供的 LibreOffice 渲染上述样例，逐页检查 6 页：第 1 页为独立索引，第 2 页的二、三、四级标题、中文小标题及正文连续排版，后续正文正常续页，模板页眉页脚背景保留。渲染结果在 `tests/section_index_tmp/pagination_render_fonts/`。运行环境默认未发现中文字体，复查时通过临时 `FONTCONFIG_FILE` 加载系统字体；该设置仅用于渲染验证，不改变转换配置或应用代码。

### 索引框自适应与标题同页修复

此前 `_index_frame()` 将单行表格的最小高度设置为正文区域的 82%。当二级标题及其段前段后距占用剩余空间时，表格会被推到下一页，即使标题设置了「与下段同页」，仍可能出现标题独占一页。现已移除最小行高，启用 `autofit`，将表格和单元格的首选宽度设为自动；保留标题的相邻约束和长索引跨页能力。

编辑前 GitNexus 评估 `_index_frame` 为 LOW：直接调用方是 `add_section_indexes`，上游为 `Converter.to_document` 和 `convert_string`。变更后 `detect_changes(scope="all")` 为 low，实际业务修改仅在索引框函数。

新增回归覆盖 1、6、100 个条目、较短模板页面、大字号多行标题、保存后重新打开，以及模板节属性和页眉页脚不变。修复前 4 项布局断言失败，修复后全量 `uv run pytest -q` 为 **306 项通过**，仅保留既有 `test_nested_lists` 返回值警告。

使用用户原 Word 模板和已保存字体配置，另将二级标题放大至 28pt、段后距设为 24pt，构造短索引、多行长标题和 32 条目跨页索引。修复前样例为 13 页，前两个索引的标题均单独占页；修复后为 11 页，第 1、3、5 页均同时包含二级标题和索引表格，长索引正常续页。渲染文件在 `tests/section_index_tmp/adaptive_after_render/`，复现脚本为 `tests/section_index_tmp/check_adaptive_layout.py`。

### 二级标题移入表格首行

索引改为单列表格：首行是二级标题，下一行是三级、四级条目；空节只创建标题行。标题仍使用 `heading2` 的字体和段落参数，表格外不再生成重复的索引标题。分页属性设置在首行的第一个段落上，使整个表格另起一页；文档开头的首个索引不加前置分页，正文二级标题仍在索引结束后另起一页。保留首行「与下段同页」、表格内容自适应、双线外框、内部无横线和长索引续页。

GitNexus 对 `_index_frame` 和 `add_section_indexes` 的编辑前评估均为 LOW，后者直接由 `Converter.to_document` 调用。回归断言已更新为检查实际的标题行、条目行、空节单行表格、表格外无索引标题以及保存后的字体和模板保留。短标题、多行标题和长索引的渲染结果在 `tests/section_index_tmp/title_inside_render/`。

最新全量回归为 306 项通过；渲染样例的 5 页索引已逐页检查，6 页正文与此前确认的渲染逐像素一致。另验证空节样例的单行标题表格和下一页正文，共 2 页；原模板完整节属性、首页/奇偶页页眉页脚均保留。

### 修复标题行与全部条目分离

用户随后提供的截图显示标题行独占一页，全部条目从下一页开始。检查最近转换的实际 DOCX，发现表格第二行包含了全部 19 个三级、四级标题；此前的 LibreOffice 样例渲染没有覆盖编辑器将这个大行整体推到下一页的表现。

索引现改为每个条目独立一行，行高自动且设置单行不跨页。标题行与首个条目保持相邻，三级条目与其首个四级条目保持相邻，其余条目允许正常分页。表格上下各保留一次 10pt 外缘内边距，中间行不叠加内边距；字体、字号和段落间距仍使用保存的配置。长索引在条目之间续页，不把全部条目绑定为一个大行。

编辑前 GitNexus 对 `_index_frame`、`add_section_indexes` 的评估均为 LOW，直接调用链为 `add_section_indexes` → `_index_frame` 和 `Converter.to_document` → `add_section_indexes`。更新后的分页结构断言在修复前有 5 项失败，修复后全量 `uv run pytest -q` 为 **307 项通过**，保留既有 `test_nested_lists` 返回值警告。回归覆盖 1、6、19、100 个条目、分组相邻约束、自动行高、外缘内边距、完整链接、保存的字体样式和模板页眉页脚。

使用最近项目首节的实际标题、原模板和已保存配置生成 `tests/section_index_tmp/actual_index_after.docx`：20 行分别保存二级标题及 19 个条目，完整节属性与模板一致。后台 LibreOffice 渲染的 5 页已逐页检查：第 2 页同时显示二级标题和前 13 个条目，第 3 页显示余下 6 个条目，第 4 页开始正文，无标题单独占页、条目遗漏或额外空白页。按用户要求不再打开 Word，此处仅报告结构检查和后台渲染结果。

最终 `detect_changes(scope="all")` 汇总为 medium，涉及 4 个预期文件和一个 `To_document → _points` 索引流程；结合实际差异核对，生产修改仅为 `_index_frame` 和 `add_section_indexes`，其余是测试及说明文档。`git diff --check` 通过。
