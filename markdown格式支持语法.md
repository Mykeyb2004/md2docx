# Markdown 格式支持语法规范

## 概述
本文档定义了 md2docx 工具需要支持的 Markdown 语法特性。

---

## ✅ 高优先级语法（必须支持）

### 1. 标题（Headings）

支持一到四级标题：

```markdown
# 一级标题
## 二级标题
### 三级标题
#### 四级标题
```

**转换规则**：
- 在 Word 中映射为"标题1"到"标题4"样式
- 保持标题层级关系，便于生成文档目录

---

### 2. 文本格式化

#### 2.1 粗体（Bold）
```markdown
**粗体文本**
```
用于强调关键词、数据、核心观点。

#### 2.2 斜体（Italic）
```markdown
*斜体文本*
```
用于补充说明、引用等。

#### 2.3 粗斜体（可选）
```markdown
***粗斜体文本***
```

---

### 3. 列表（Lists）

#### 3.1 有序列表
```markdown
1. 第一项
2. 第二项
3. 第三项
```

#### 3.2 无序列表
```markdown
* 列表项一
* 列表项二
- 列表项三
- 列表项四
```

**说明**：支持 `*` 和 `-` 两种标记符号。

#### 3.3 多级嵌套列表
```markdown
1. 一级有序列表
    * 二级无序列表
    * 二级无序列表
        1. 三级有序列表
        2. 三级有序列表
2. 一级有序列表
    1. 二级有序列表
        * 三级无序列表
```

**转换规则**：
- 支持最多 4 级嵌套
- 正确识别缩进（通常为 4 个空格或 1 个 Tab）
- 保持列表项的层级关系

---

### 4. 表格（Tables）

```markdown
| 列标题1 | 列标题2 | 列标题3 |
| :------ | :------ | :------ |
| 内容1   | 内容2   | 内容3   |
| 内容4   | 内容5   | 内容6   |
```

**支持特性**：
- 列对齐方式：
  - `:------` 左对齐
  - `:-----:` 居中对齐
  - `------:` 右对齐
- 表格单元格内支持**粗体**、*斜体*等内联格式
- 自动调整列宽

---

### 5. 水平分隔线（Horizontal Rule）

```markdown
---
```

用于章节分隔，转换为 Word 中的水平线。

---

### 6. 段落与换行

```markdown
这是第一段落。

这是第二段落（段落之间用空行分隔）。

这是同一段落的第一行
这是同一段落的第二行（无空行则连续）
```

**转换规则**：
- 空行分隔段落
- 保持段落的连续性

---

## ⭐ 中优先级语法（推荐支持）

### 7. 混合格式

支持在复杂结构中嵌套格式，例如：
- 列表项中的**粗体**和*斜体*
- 表格单元格中的格式化文本
- 标题中的格式化（如 `### **加粗的标题**`）

---

### 8. 特殊符号与字符

保留并正确转换：
- 中文标点符号：，。！？：；""''《》
- 英文标点符号：,.!?:;"'
- 数学符号：%、元、亿、万
- 特殊字符：©、®、™

---

## 📌 低优先级语法（可选扩展）

### 9. 引用标记（上标/下标）

```markdown
文本¹  （上标引用）
H₂O   （下标）
```

**建议**：如果不支持上标，可转换为方括号形式 `[1]`。

---

### 10. 行内代码（可选）

```markdown
`代码片段`
```

虽然研究报告中未使用，但技术文档可能需要。

---

### 11. 代码块（可选）

````markdown
```python
def hello():
    print("Hello, World!")
```
````

技术文档可能需要。

---

### 12. 链接和图片（可选）

```markdown
[链接文本](https://example.com)
![图片描述](image.png)
```

---

## 📊 转换优先级总结

| 优先级 | 语法特性 | 使用频率 | 实现难度 |
| :----: | :------- | :------: | :------: |
| 🔴 高 | 一到四级标题 | 极高 | 低 |
| 🔴 高 | 粗体文本 | 极高 | 低 |
| 🔴 高 | 有序/无序列表 | 极高 | 中 |
| 🔴 高 | 多级嵌套列表 | 高 | 中 |
| 🔴 高 | 表格 | 高 | 高 |
| 🔴 高 | 水平分隔线 | 中 | 低 |
| 🔴 高 | 段落与空行 | 极高 | 低 |
| 🟡 中 | 斜体文本 | 中 | 低 |
| 🟡 中 | 混合格式 | 中 | 中 |
| 🟢 低 | 上标/下标 | 低 | 中 |
| 🟢 低 | 代码块 | 低 | 中 |
| 🟢 低 | 链接/图片 | 低 | 高 |

---

## 🎯 实现建议

### 阶段一：核心功能（MVP）
实现所有 🔴 高优先级语法，确保能够正确转换研究报告类文档。

### 阶段二：完善功能
添加 🟡 中优先级语法，提升文档格式的丰富性。

### 阶段三：扩展功能
根据实际需求，选择性实现 🟢 低优先级语法。

---

## 📝 测试用例参考

建议使用 `研究报告 v01.md` 作为主要测试文档，该文档包含：
- ✅ 三、四级标题
- ✅ 大量粗体强调
- ✅ 复杂的嵌套列表（3-4 层）
- ✅ 多个表格（含对齐方式）
- ✅ 水平分隔线
- ✅ 混合中英文和数字
- ✅ 上标引用标记

---

**文档版本**：v1.0  
**编制日期**：2025-11-19  
**最后更新**：2025-11-19
